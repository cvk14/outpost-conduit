/* ==========================================================================
   Outpost Conduit — Traffic View (stub)
   ========================================================================== */

window.TrafficView = {
  render(container) {
    // Static stub content — no user input, safe to use innerHTML
    container.innerHTML = `
      <div class="container">
        <h2>Traffic</h2>
        <p class="subtitle">Loading...</p>
      </div>
    `;
  },
};
