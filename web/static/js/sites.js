/* ==========================================================================
   Outpost Conduit — Sites View (stub)
   ========================================================================== */

window.SitesView = {
  render(container) {
    // Static stub content — no user input, safe to use innerHTML
    container.innerHTML = `
      <div class="container">
        <h2>Sites</h2>
        <p class="subtitle">Loading...</p>
      </div>
    `;
  },
};
