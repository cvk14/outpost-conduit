/* ==========================================================================
   Outpost Conduit — Deploy View (stub)
   ========================================================================== */

window.DeployView = {
  render(container) {
    // Static stub content — no user input, safe to use innerHTML
    container.innerHTML = `
      <div class="container">
        <h2>Deploy</h2>
        <p class="subtitle">Loading...</p>
      </div>
    `;
  },
};
