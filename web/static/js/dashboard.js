/* ==========================================================================
   Outpost Conduit — Dashboard View (stub)
   ========================================================================== */

window.DashboardView = {
  render(container) {
    // Static stub content — no user input, safe to use innerHTML
    container.innerHTML = `
      <div class="container">
        <h2>Dashboard</h2>
        <p class="subtitle">Loading...</p>
      </div>
    `;
  },
};
